#include <bits/stdc++.h>
#include <stdlib.h>
using namespace std;

int main() {
    double *p;
    int n;
    cout<< "\n So phan tu : "; cin>>n;
    p = new double[n];
    if (p == NULL)
    {
    cout << "Loi cap phat bo nho";
    exit(0);
    }   
}